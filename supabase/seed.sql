-- seed.sql — placeholder.
-- Plan SC: "DB dump produces zero plaintext credentials".
-- That invariant applies to real user data. Test seed values must therefore use SYNTHETIC ciphertext
-- generated by core-crypto in module-4 test fixtures.
--
-- We do NOT insert any rows here, because:
--   1. The auth.users table is managed by Supabase Auth and seeded via the dashboard or test fixtures.
--   2. public.users rows must be created AFTER signup, with KDF params derived client-side.
--
-- Tests will populate seed data via `packages/vault-sdk/test/seed.ts` (module-4).
select 'seed.sql is intentionally empty — see packages/vault-sdk/test/seed.ts in module-4' as note;
